package com.oceanum.exec.spark

import com.oceanum.exec.OperatorProp

/**
 * @author chenmingkun
 * @date 2020/5/10
 */
case class SparkProp() extends OperatorProp {
  override def close(): Unit = ???
}
